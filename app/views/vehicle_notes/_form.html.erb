<%= form_with(model: vehicle_note) do |form| %>
  <% if vehicle_note.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vehicle_note.errors.count, "error") %> prohibited this vehicle_note from being saved:</h2>

      <ul>
        <% vehicle_note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.label :vehicle_id, style: "display: block" %>
    <%= form.text_field :vehicle_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
