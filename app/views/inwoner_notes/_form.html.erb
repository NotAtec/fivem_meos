<%= form_with(model: inwoner_note) do |form| %>
  <% if inwoner_note.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(inwoner_note.errors.count, "error") %> prohibited this inwoner_note from being saved:</h2>

      <ul>
        <% inwoner_note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :inwoner_id, style: "display: block" %>
    <%= form.text_field :inwoner_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
